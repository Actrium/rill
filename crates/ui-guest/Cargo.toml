[package]
name = "ui-guest"
version.workspace = true
edition.workspace = true
description = "Demo native rill guest that renders UI (View > Text) via the rill-guest SDK."
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rill-guest = { path = "../rill-guest" }
