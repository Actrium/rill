[package]
name = "kv-guest"
version.workspace = true
edition.workspace = true
description = "Demo native rill guest: calls host:store.putText through the rill-guest SDK."
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rill-guest = { path = "../rill-guest" }
